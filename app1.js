/*import React from "react";
import ReactDOM from "react-dom/client"

// REACT ELEMENT
const Heading=(
<h1 className="head" tabIndex="5">Namaste REACT USING JSx 
</h1>);

// REACT COMPONENTS
// class based component=old
// functional component=new

// REACT functional component
const HeadingComponent=()=>{
    return <h1 className="heading">Namaste REACT USING JSx </h1>;
};
// OR
const HeadingComponen1t=()=>( <div id="container">
<h1 className="heading">Namaste REACT USING JSx </h1> </div>);

const root=ReactDOM.createRoot(document.getElementById("root"));
root.render(Heading);// this is a way to render react element
   

root.render(<HeadingComponen1t/>);// this is the way to render react component
*/